import { LegendPosition } from "./legendInterfaces";
import { InteractiveLegend } from "./interactiveLegend";
import { SVGLegend } from "./svgLegend";
export function createLegend(legendParentElement, interactive, interactivityService, isScrollable = false, legendPosition = LegendPosition.Top, interactiveBehavior) {
    if (interactive) {
        return new InteractiveLegend(legendParentElement);
    }
    return new SVGLegend(legendParentElement, legendPosition, interactivityService, isScrollable, interactiveBehavior);
}
export function isLeft(orientation) {
    switch (orientation) {
        case LegendPosition.Left:
        case LegendPosition.LeftCenter:
            return true;
        default:
            return false;
    }
}
export function isTop(orientation) {
    switch (orientation) {
        case LegendPosition.Top:
        case LegendPosition.TopCenter:
            return true;
        default:
            return false;
    }
}
export function positionChartArea(chartArea, legend) {
    let legendMargins = legend.getMargins(), legendOrientation = legend.getOrientation();
    chartArea.style("margin-left", isLeft(legendOrientation)
        ? legendMargins.width + "px"
        : null);
    chartArea.style("margin-top", isTop(legendOrientation)
        ? legendMargins.height + "px"
        : null);
}
//# sourceMappingURL=legend.js.map